edition: 3.0.0
name: fortune-teller
access: default

resources:
  fortune-teller:
    component: fc3
    props:
      region: cn-hangzhou
      service:
        name: fortune-teller-service
      function:
        name: web-function  # ← 关键：必须有这个
        runtime: nodejs18
        code: ./
        handler: .next/server/index.handler
        memorySize: 1024
        timeout: 30
      triggers:
        - type: http
          name: default
          properties:
            authType: anonymous
            methods: ['GET', 'POST']